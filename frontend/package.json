{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build && node -e \"require('fs').closeSync(require('fs').openSync('dist/.gitkeep','a'))\"",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.2.37"
  },
  "devDependencies": {
    "@babel/types": "^7.18.10",
    "@vitejs/plugin-vue": "^3.0.3",
    "typescript": "~5.3.3",
    "vite": "^3.0.7",
    "vue-tsc": "^1.8.27"
  }
}
